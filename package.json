{
  "name": "fullstack-app",
  "version": "1.0.0",
  "private": true,
  "description": "Fullstack app: Express + MongoDB + Angular",
  "scripts": {
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "dev": "concurrently -n api,web -c blue,green \"npm run dev --prefix backend\" \"npm start --prefix frontend\"",
    "dev:api": "npm run dev --prefix backend",
    "dev:web": "npm start --prefix frontend",
    "build": "npm run build --prefix frontend",
    "start:api": "npm start --prefix backend",
    "docker:db": "docker compose up -d"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
